<div id="<%= dom_id(order, :status) %>" class="p-4 rounded-2xl text-center <%= order.status_color %>">
  <p class="text-sm font-medium opacity-70">Status</p>
  <p class="text-xl font-bold mt-0.5"><%= order.status_label %></p>
</div>
